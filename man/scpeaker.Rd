% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scpeaker.R
\name{scpeaker}
\alias{scpeaker}
\title{Run CellChat (v2) and/or CellOPhoneDB (v5) for cell-cell communication analysis}
\usage{
scpeaker(
  obj,
  labels,
  method = c("cellchat", "cellphonedb"),
  database = c("scpeakerDB", "CCDB", "CPDB"),
  pvalue = 0.05,
  assay = "RNA",
  subsetDB = FALSE,
  search = c(),
  key = "annotation",
  non_protein = FALSE,
  type = c("triMean", "truncatedMean", "thresholdedMean", "median"),
  threshold = 0.1,
  LR.use = NULL,
  raw.use = TRUE,
  population.size = FALSE,
  distance.use = TRUE,
  interaction.range = 250,
  scale.distance = 0.01,
  k.min = 10,
  contact.dependent = TRUE,
  contact.range = NULL,
  contact.knn.k = NULL,
  contact.dependent.forced = FALSE,
  do.symmetric = TRUE,
  nboot = 100,
  seed.use = 1L,
  Kh = 0.5,
  n = 1,
  min.cells = 10,
  use_dir = NULL,
  ...,
  counts_data = "hgnc_symbol",
  active_tfs_file_path = NULL,
  microenvs_file_path = NULL,
  score_interactions = FALSE,
  subsampling = FALSE,
  subsampling_log = FALSE,
  separator = "|",
  debug = FALSE,
  output_suffix = NULL
)
}
\arguments{
\item{obj}{Seurat object}

\item{labels}{Metadata column name for the cell type labels}

\item{method}{Cell-cell communication method for analysis (CellChat or CellPhoneDB)}

\item{database}{Database to use for cell-cell communication analysis (default: scpeakerDB)}

\item{pvalue}{p-value threshold for significance testing (default: 0.05)}

\item{assay}{RNA assay by default}

\item{subsetDB}{set to TRUE if filtering scpeakerDB (default = FALSE). If TRUE, use search, key and non_protein arguments as in help(subsetDB)}

\item{search}{taken from CellChat package, run help(subsetDB) for details}

\item{key}{taken from CellChat package, run help(subsetDB) for details}

\item{non_protein}{taken from CellChat package, run help(subsetDB) for details}

\item{type}{taken from CellChat package, run help(computeCommunProb) for details}

\item{threshold}{taken from CellChat package and replaces "trim" argument, run help(computeCommunProb) for details}

\item{LR.use}{taken from CellChat package, run help(computeCommunProb) for details}

\item{raw.use}{taken from CellChat package, run help(computeCommunProb) for details}

\item{population.size}{taken from CellChat package, run help(computeCommunProb) for details}

\item{distance.use}{taken from CellChat package, run help(computeCommunProb) for details}

\item{interaction.range}{taken from CellChat package, run help(computeCommunProb) for details}

\item{scale.distance}{taken from CellChat package, run help(computeCommunProb) for details}

\item{k.min}{taken from CellChat package, run help(computeCommunProb) for details}

\item{contact.dependent}{taken from CellChat package, run help(computeCommunProb) for details}

\item{contact.range}{taken from CellChat package, run help(computeCommunProb) for details}

\item{contact.knn.k}{taken from CellChat package, run help(computeCommunProb) for details}

\item{contact.dependent.forced}{taken from CellChat package, run help(computeCommunProb) for details}

\item{do.symmetric}{taken from CellChat package, run help(computeCommunProb) for details}

\item{nboot}{taken from CellChat package, run help(computeCommunProb) for details}

\item{seed.use}{taken from CellChat package, run help(computeCommunProb) for details}

\item{Kh}{taken from CellChat package, run help(computeCommunProb) for details}

\item{n}{taken from CellChat package, run help(computeCommunProb) for details}

\item{min.cells}{taken from CellChat package, run help(filterCommunication) for details}

\item{use_dir}{Input files required to run CellPhoneDB and the output files from the analysis are created and saved to a temp file (default). Should users desire to have these files be stored in their personal directories, they may input their filepath to this argument.}

\item{...}{Takes in any arguments from c("iterations", "threads", "debug_seed", "result_precision", "subsampling_num_pc", "subsampling_num_cells") for user customization, else running on the default values for these paramters. See Value below for link to description by CellPhoneDB}

\item{counts_data}{See Value below for link to description by CellPhoneDB}

\item{active_tfs_file_path}{See Value below for link to description by CellPhoneDB}

\item{microenvs_file_path}{See Value below for link to description by CellPhoneDB}

\item{score_interactions}{See Value below for link to description by CellPhoneDB}

\item{subsampling}{See Value below for link to description by CellPhoneDB}

\item{subsampling_log}{See Value below for link to description by CellPhoneDB}

\item{separator}{See Value below for link to description by CellPhoneDB}

\item{debug}{See Value below for link to description by CellPhoneDB}

\item{output_suffix}{See Value below for link to description by CellPhoneDB}
}
\value{
Cell-cell communication inference results from the chosen method (CellChat object for CellChat analysis and CellPhoneDB object list).
}
\description{
This function takes as input a Seurat object that has been processed and contains annotated cell type labels in its meta.data.

It uses scpeakerDB which contains interactions from both CellChat and CellPhoneDB as the default database, but users may customize the resource to CellChat/CellPhoneDB only interactions.
}
\section{CellChat arguments}{

}

\section{CellPhoneDB arguments}{

}

\examples{
\dontrun{
cellchat <- scpeaker(seu.NL, labels = "labels", method = "cellchat")

cpdb     <- scpeaker(seu.NL, labels = "labels", method = "cellphonedb")
}

}
